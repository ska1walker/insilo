apiVersion: v2
name: insilo
description: Datensouveräne Meeting-Intelligenz für deutschen Mittelstand
type: application
version: 0.1.36          # Chart-Version (Semver) — bei jeder Chart-Änderung erhöhen
appVersion: "0.1.36"     # Upstream-Software-Version (= spec.versionName im OlaresManifest) — Marc's Golden Rule: muss = version sein
